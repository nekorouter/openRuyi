diff -ruN a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt	2026-06-17 17:19:09
+++ b/CMakeLists.txt	2026-06-17 17:19:11
@@ -256,37 +256,12 @@
     MLIRPluginsLib
 
     # LLVM
-    LLVMPasses
-    LLVMNVPTXCodeGen
-    # LLVMNVPTXAsmPrinter
-    LLVMAMDGPUCodeGen
-    LLVMAMDGPUAsmParser
+    LLVM
 
     Python3::Module
     pybind11::headers
 
   )
-  if(CMAKE_SYSTEM_PROCESSOR MATCHES "aarch64" OR # Linux arm64
-     CMAKE_SYSTEM_PROCESSOR MATCHES "arm64" OR # macOS arm64
-     CMAKE_OSX_ARCHITECTURES MATCHES "arm64")  # also macOS arm64
-      list(APPEND TRITON_LIBRARIES
-          LLVMAArch64CodeGen
-          LLVMAArch64AsmParser
-      )
-  elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "x86_64" OR CMAKE_SYSTEM_PROCESSOR MATCHES "AMD64")
-      list(APPEND TRITON_LIBRARIES
-          LLVMX86CodeGen
-          LLVMX86AsmParser
-      )
-  elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "ppc64le")
-      list(APPEND TRITON_LIBRARIES
-        LLVMPowerPCAsmParser
-        LLVMPowerPCCodeGen
-      )
-  else()
-    message(FATAL_ERROR "LLVM codegen/ASM parser libs: This HW architecture (${CMAKE_SYSTEM_PROCESSOR}) is not configured in cmake lib dependencies.")
-  endif()
-
   # Define triton library
   string(JOIN "," TRITON_BACKENDS_TUPLE ${TRITON_CODEGEN_BACKENDS})
 
diff -ruN a/cmake/FindLLVM.cmake b/cmake/FindLLVM.cmake
--- a/cmake/FindLLVM.cmake	2026-05-08 00:50:13
+++ b/cmake/FindLLVM.cmake	2026-06-17 17:19:11
@@ -80,7 +80,7 @@
         endif()
         set(result_code)
         execute_process(
-            COMMAND ${LLVM_CONFIG} --link-static --${flag}
+            COMMAND ${LLVM_CONFIG} --link-shared --${flag}
             RESULT_VARIABLE result_code
             OUTPUT_VARIABLE LLVM_${var}
             OUTPUT_STRIP_TRAILING_WHITESPACE
@@ -100,7 +100,7 @@
         endif()
         set(result_code)
         execute_process(
-            COMMAND ${LLVM_CONFIG} --link-static --${flag} ${components}
+            COMMAND ${LLVM_CONFIG} --link-shared --${flag} ${components}
             RESULT_VARIABLE result_code
             OUTPUT_VARIABLE tmplibs
             OUTPUT_STRIP_TRAILING_WHITESPACE
